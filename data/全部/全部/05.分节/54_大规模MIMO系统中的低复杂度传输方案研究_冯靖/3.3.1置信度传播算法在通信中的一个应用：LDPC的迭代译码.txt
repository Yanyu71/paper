3.3.1置信度传播算法在通信中的一个应用：LDPC的迭代译码
算法需要一个初始化的步骤，为是接收符号先验信息估计值的集合，其中的元 素咛表示第7个符号的概率值是冷 这个信息由信道的模型决定，在AWGN信道下, 符号的先验信息由高斯概率密度函数决定。初始化后，符号与校验节点的开始信息 交换。每个校验节点生向其符号节点内传递的信息记为R力，这个概率是当符号节 点处于状态x时，满足校验节点小 （每个校验节点对应一个校验方程）得到的一个
16
概率值。满足校验方程的概率值求解如下式
P(hi/dj=x) = £ p(hi/d)p(d/dj=x) d:dj=x
(3-1)
当符号节点处于状态无时，计算满足校验方程所有可能的译码向量a的概率值。对 于校验节点生,当其取不同的X时，它向符号节点为.传递信息的值由下式计算
舫二e「仇㈤n Qi d:dj=x	k£N (i)\j
(30
在上式中，N(i)代表所有与校验节点后相连的父符号节点下标值的集合，代 表除了符号节点外其他所有节点的下标值集合。对于一个给定的向量d,如果满 足校验方程，那么P(也/d)的概率值是1,否则为零。当符号节点的值为x,符号节点 dj向校验节点%传递信息为蜀,这个信息由与此符号节点相连的其他校验节点提 供的信息计算得到。那么，根据贝叶斯准则，
P @ = x) P (g}= x)
(3-3)
那么符号节点为向校验节点传递的信息由下式计算
4=%右n版
(3-4)
其中M(j)代表所有与符号节点dj相连的校验节点下标值的集合。力表示dj状态为 x的概率值，归一化参数均使得满足归一化方程匕嘴.=1。这样，参数Q各的计算 使得我们可以进一步计算参数峭，进而可以利用与对下标为j的每个值进行估计。 最后可以得到对接收向量每个符号值为。或者1的概率值的估计，估计值的计算如 下式
dj = arg max	(3-5)
如果估计的译码向量2满足特定的方程，那么认为码子正确。然而，如果译码器达 到了预先设定的迭代次数后，并没有找到一个合适的码子满足特定的方程，那么也 认为每个符号得到了最佳估计。
17
