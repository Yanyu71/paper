3.4.2回传容量分配
回传容量分配问题等价于矩阵C的求取问题。假设己经知道了协作策略的 分配结果，即矩阵S已知。通过将式(3.13)代入式(3.17)中，可以知道式(3.17) 中的目标函数是非凸的，因此很难在多项式时间复杂度内获取一个全局最优解。.
在本节中，提出了一个两阶段的次优回传容量分配算法，它的时间复杂度 为0(心3)。算法流程参见2。
Algorithm 2两阶段回传容量分配算法	’
1：根据目标基站的不同，M(M-l)条回传链路被分为M组，每组包含 条以相同目标基站的回传链路。
2：阶段一：将总回传容量。在链路组中进行分配。每个链路组获得的容量正 比于该组内有效链路的数量，分配过程如式(3.20)所示。
3:阶段二：在每个链路组内，将该链路组获得的总容量在各链路间进行更精 细的分配，各链路获得的容量如式(3.23)所示。
与协作策略选取中的协作链路的分组过程相同，根据目标基站的不同，将
M(M-l)个协作链路分为M组，每组包含M-1个协作链路。回传容量首先
27
以组为单位进行分配(组间分配)，然后再在各组内部的协作链路间进行细分 (组内分配)。
