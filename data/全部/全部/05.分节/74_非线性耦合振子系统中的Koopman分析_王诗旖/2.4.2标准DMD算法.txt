2.4.2标准DMD算法
近年来又逐渐发展了很多通过数据集来计算Koopman本征值，本征函数，模 式的相关方法。一大部分这些方法都是属于一类叫做动力学模式分解(DMD)的 算法。这个算法首先由Schimid在2010年正式提出口叱从这以后，很多DMD算 法的版本被研究用来有效的计算Koopman算符的谱画叫63］皿。在Schimid的开 创性成果之前，一些类基于Koopman算符理论的类似于DMD的算法就已经被 设计出来了，比如上一节提到的Arnoldi算法，然而基于SVD的DMD算法带来 了特别大的数值稳定性,现在已经成为DMD的标准算法，在接下来将详细说明。
如果有一组连续数据向量{z°,zi，…,z„J，zk e 向量次是从连续演化的时 间序列z(t)中抽样得到的，所以有Zk = z(kAt), At为抽样间隔。假设这组数据由 线性系统产生：
zk+i =他卜	(2-17)
对非线性系统产生的数据使用DMD算法，其实是假设存在一个线性算符A来 近似非线性系统的动力学。求得的DMD模式和特征值数值近似于A的特征值 和特征向量。
首先构造两组数据矩阵分别为：
K = {Zo …Zm_1}，L = {Z1 …zm］	(2-18)
接下来对K进行SVD分解，K = U1V*,在这里进行SVD分解的原因是，对于高 维的系统，矩阵K是非常大型的矩阵，并且其中存在大量的冗余，这将会耗费大 量的计算资源。SVD可以通过快照的方式进行高效的计算［6叫 对K进行SVD分 解后，会发现E的对角线上的元素，前面很少一部分的值就占据了几乎全部的能 量，而且矩阵U和U都是正交矩阵，选取前r列的元素所对应的U的列做为新的基， 这样就对原来的空间进行了降维。此时U为nxr的矩阵，E为rxr的矩阵，U为 rxm的矩阵。U的每一列包含了新的空间结构，P的每一列包含了数据的时间结 构。定义矩阵⑷］：
A = U*WL	(2-19)
16
然后计算矩阵区的特征值和特征向量，有：
Aw = Aw	(2-20)
这是在降维后新的空间中得到的特征值和特征向量，回到原始空间力的特征值；I 对应的特征向量枢为：
(P = Uw	(2-21)
