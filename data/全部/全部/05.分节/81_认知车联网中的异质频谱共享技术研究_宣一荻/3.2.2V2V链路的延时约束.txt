3.2.2V2V链路的延时约束
考虑到车辆的延时性能要求，本章通过建立V2V链路的端到端延时模型。
假设车辆用户产生的数据包首先进入先进先出(First In First Out, FIFO)的缓存 中，然后依次通过无线信道传输。因此，可以将V2V链路缓存中第尸个数据包 的端到端延时表示为
兀- ％ (尸)	(3_6)
其中，&(门表示V2V链路/”缓存中第尸个数据包离开的时间，气(尸)表示V2V 链路厶缓存中第尸个数据包到达/产生的时间。经分析，数据包的端到端延时包括 等待延时和传输延时，通过采用最大卷积原则〔旳，可以将端到端延时模型重新表 示为
Tt (r) = WI (r) + rz (r)	®7)
22
其中，％（门为第厂数据包的传输延时，肌⑴为V2V链路Z”缓存中第尸个数据包 的等待延时，表不为
［一1	（3-8）
化（2衆驚［工巧"）-工% （切
k—v	k=v
其中，q仇）为第伙+1）个数据包和第k个数据包之间的到达间隔。
然后，V2V链路厶的延时约束条件表示为
P{rin（r）>tma^<S,n = l,2,...,L	S）
即每个数据包端到端延时超过V2V链路最大数据包端到端延时门限/嘶的概率 小于最大延时溢出概率门限£ o
